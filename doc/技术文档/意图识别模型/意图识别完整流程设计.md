用户输入
  ↓
规则预处理
  ↓
意图识别模型
  ↓
输出 TopK 意图候选
  ↓
判断 Top1 / Top2 是否接近
  ↓
模型槽位抽取
  ↓
实体候选召回
  ↓
意图-槽位-实体一致性检查
  ↓
能裁决？
  ├─ 是 → 输出 final_intent + final_slots → 路由工具
  └─ 否 → 触发澄清

疑似存在问题：
1.南京112 如何保证命中船舶 而不是命中地址 可以训练模型拥有对应的分辨能力

2.上下文意图识别 多轮上下文的时候意图混乱问题

第一轮：基础训练 / 基线模型 获得基础意图识别的能力
第二轮：针对 bad case 的增强训练 / 难例训练 / 继续微调  整理我们失败的样本集进行优化训练